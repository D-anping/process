#include <stdio.h>
int n,i,a,b;
int main()
{
    while (scanf("%d",&n)!=EOF && n!=0)
    {
        for (i=1;i<=n;i++)
        {
            scanf("%d",&a);
            b=b+a;
        }
        printf("%d\n",b);
        b=0;
    }
    return 0;
}
