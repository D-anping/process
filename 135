#include <stdio.h>
#include <string.h>
char ch[81];
int n,a,e,i,o,u,j,k,p;
int main()
{
	while (gets(ch)!=NULL)
	{
		n=strlen(ch);
		for (j=0;j<n/2;j++)
		{
			if (ch[j]!=ch[n-1-j])
			{
				break;
			}
		}
		if (j==n/2)
		{
			printf("Yes\n");
		}
		else
		{
			printf("No\n");
		}
	}
	return 0;
}
