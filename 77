#include <stdio.h>
int n,i,a,sum;
int main()
{
	while (scanf("%d",&n)!=EOF)
	{
		sum=1;
		for (i=1;i<=n;i++)
		{
			scanf_s("%d",&a);
			if (a%2!=0)
			{
				sum=sum*a;
			}
		}
        printf("%d\n",sum);
	}
	return 0;
}
