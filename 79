#include <stdio.h>
#include <math.h>
float a,sum;
int m,i,n;
int main()
{
	while (scanf("%d %d",&n,&m)!=EOF)
	{
		if (n<10000 && m<1000)
		{
			sum=0;
			sum=sum+n;
			a=sqrt(n);
			for (i=1;i<m;i++)
			{
				sum=sum+a;
				a=sqrt(a);
			}
			printf("%.2f\n",sum);
		}
	}
	return 0;
}
