#include <stdio.h>
int n,*p=&n,a[200],*p1=a,i;
int main()
{
	scanf("%d",p);
	for (i=0;i<n;p1++,i++)
	{
		scanf("%d",p1);
	}
	printf("%d",a[n-1]);
	for (i=n-2;i>=0;i--)
	{
		printf(" %d",a[i]);
	}
	printf("\n");
	return 0;
}
