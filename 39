#include <stdio.h>
int m,n,t,a,b;
int main()
{
	scanf("%d %d",&n,&m); 
	t=m-n*2;
	a=t/2;
	b=n-a;
	if (t%2!=0 || m<0 || n<0 || t<0 || b<0)
	{
		printf("No");
	}
	else
	{
		printf("%d %d",b,a);
	}
	return 0;
}
