#include <stdio.h>
#include <math.h>
int n,a,b,c;
int main()
{
	while (scanf("%d",&n)!=EOF)
	{
		c=n%10;
		b=n/10%10;
		a=n/100;
		if ((a*a*a+b*b*b+c*c*c)==(a*100+b*10+c))
		{
			printf("Yes\n");
		}
		else
		{
			printf("No\n");
		}
	}
	return 0;
}
