#include <stdio.h>
#include <string.h>
int n,i; 
char ch[101];
int main()
{
	while (gets(ch)!=NULL)
	{
		n=strlen(ch);
		for (i=n-1;i>=0;i--)
		{
			printf("%c",ch[i]);
			if (i==0)
			{
                printf("\n");
			}
		}
	}
	return 0;
}
