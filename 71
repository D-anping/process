#include <stdio.h>
#include <math.h>
int n,a,b,c,d,e;
int main()
{
	while (scanf("%d",&n)!=EOF)
	{
		e=n%10;
		d=n/10%10;
		c=n/100%10;
		b=n/1000%10;
		a=n/10000;
		if ((pow(a,5)+pow(b,5)+pow(c,5)+pow(d,5)+pow(e,5))==n)
		{
			printf("Yes\n");
		}
		else
		{
			printf("No\n");
		}
	}
	return 0;
}
