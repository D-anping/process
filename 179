#include <stdio.h>
int a[100001],n,i,t=1,max;
int *p1=&n,*p2=a;
int main()
{
	scanf("%d",p1);
	for(;i<n;i++,p2++)
	{
		scanf("%d",p2);
	}
	max=a[0];
	for (i=1;i<n;i++)
	{
		if (a[i]>max)
		{
			max=a[i];
			t++;
		}
	}
	printf("%d\n",t);
	return 0;
}
