#include <stdio.h>
int n,b,i,sum;
int main()
{
	while (scanf("%d",&n)!=EOF)
	{
		if (n<10 && n>0)
		{
			sum=0;
			b=1;
			for (i=1;i<=n;i++)
			{
				b=b*i;
				sum=b+sum;
			}
			printf("%d\n",sum);
		}
	}
	return 0;
}
