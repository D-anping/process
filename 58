#include <stdio.h>
int n,i;
int main()
{
	while(scanf("%d",&n)!=EOF)
	{
		for (i=1;i<=n;i++)
		{
			if (i%3==0)
			{
				printf("%d",i);
				if (i+3<=n)
				{
					printf(" ");
				}
			}
		}
		printf("\n");
	}
	return 0;
}
