#include <stdio.h>
int i,n,j,max,t,a;
int main()
{
	scanf("%d",&n);
	for (i=1;i<=n;i++)
	{
		scanf("%d",&t);
		for (j=1;j<=t;j++)
		{
			scanf("%d",&a);
			if (j==1)
			{
				max=a;
			}
			else if (a>max)
			{
				max=a;
			}
		}
		printf("%d\n",max);
	}
	return 0;
}
