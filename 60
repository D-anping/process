#include <stdio.h>
int i,n,j,min,t,a;
int main()
{
	scanf("%d",&n);
	for (i=1;i<=n;i++)
	{
		scanf("%d",&t);
		for (j=1;j<=t;j++)
		{
			scanf("%d",&a);
			if (j==1)
			{
				min=a;
			}
			else if (a<min)
			{
				min=a;
			}
		}
		printf("%d\n",min);
	}
	return 0;
}
