#include <stdio.h>
int i,sum,n;
int main()
{
	scanf("%d",&n);
	for (i=1;i<n;i++)
	{
		if (n%i==0)
		{
			sum=sum+i;
		}
	}
	if (sum==n)
	{
		printf("%d is cloze.",n);
	}
	else
	{
		printf("%d is not cloze.",n);
	}
	return 0;
}
